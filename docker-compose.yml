services:
  mongodb:
    image: mongo:8.2
    container_name: mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=ai-tools-catalog

volumes:
  mongodb_data:
    driver: local